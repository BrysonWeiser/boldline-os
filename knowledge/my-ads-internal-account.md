---
name: my-ads-internal-account
topic: OS app
task: run ads for BoldLine itself via the "My Ads" house account (internal, not a client) and how it's kept out of business metrics
keywords: [my ads, internal, house account, makeInternalClient, realClients, myAccount, dogfood, first client, google launch, self client, run own ads, MyAdsSetupCards, monthly budget, NicheSelect, niche catalog, custom niche, payment card, connect accounts, Customer ID, log tab removed]
status: verified
summary: BoldLine runs its OWN ads (and dogfoods the whole client flow) via a "My Ads" house account — a client record flagged `internal:true` that reuses ALL the client machinery (Package Google/Meta launch cards, landing pages, leads, ARIA) but is kept OUT of the client list + MRR/revenue/count/billing, and has NO Client-Portal or Contract tab. Reached from its own "My Ads" section (dashboard card + desktop sidebar item). Built 2026-07-24 (Bryson: run ads for myself + be the first test client). Doubles as the first real Google account linked to the MCC → the live E2E test of the launch card + approve→execute.
verified: 2026-08-07
---

**Why (Bryson, 2026-07-24):** run BoldLine's own ads to get clients, AND use BoldLine as the first client/test subject to prove the whole build works on a real account. Answers to the design question: reuse the client machinery, but NOT listed as a regular client — give it its own "My Ads" section, show all the ad info in its tabs, and **no client portal or contract** (not needed for self). Exclude it from all business metrics.

**The model — `internal:true`:** `makeInternalClient()` (index.html, just above `AddClientSheet`) builds a normal client record flagged `internal:true`, `name:"BoldLine Media"`, `packageId:"c-growth"` (combined → BOTH Google + Meta launch cards show on the Package tab), `stage:"active"`, `intakeComplete/contractSigned:true`, no contract dates. Stored in Supabase as `data` like any client, so the flag round-trips automatically. Created on first open (there's normally exactly one).

**Kept out of the client list + metrics:** in the App component, `realClients = clients.filter(c=>!c.internal)` and `myAccount = clients.find(c=>c.internal)`. `realClients` is passed to HomeScreen / RevenueScreen / SegmentScreen, so MRR, revenue, active/client counts, the client list, alerts, and expiring all exclude the house account. ARIA's STATE line (MRR/counts) also uses a local `realClients`; ARIA's CLIENTS list still includes it but tagged `[INTERNAL: … never propose upgrades/billing/contract … don't count as revenue]` so ARIA can help optimize its ads without treating it as billable. The live-ad readers + ad execution still run over the FULL `clients` (so its campaigns are managed normally).

**Its own "My Ads" section (entry points):**
- A distinct **dashboard card** at the top of Home (`🚀 My Ads — BoldLine Media`) — "Set up ›" when it doesn't exist yet (creates it), "Open ›" once it does. `onOpenMyAds` = `if(myAccount) selectClient(myAccount) else addClient(makeInternalClient())`.
- A **desktop sidebar item** "My Ads" (target icon), highlighted when the open client is internal (`myAdsActive`).
- No mobile bottom-nav item (kept the bar uncrowded — the dashboard card is the mobile entry).

**Its detail view (ClientHub) when `client.internal`:**
- **Tabs:** portal ("Client View") and contract are removed → Overview / Pipeline / Leads / Package / Reports / Log only.
- **Header badge:** a gold "MY ADS" pill + "Internal — house account" chip; health score + contract "days left" chips hidden.
- **Overview tab:** the client-billing stuff is hidden (Monthly Revenue card, Health Score breakdown, Contract Timeline, Onboarding Checklist, and the Health/Contract-End/Revenue stat tiles). Shows ad-relevant tiles only (Platform, Ad Budget, Niche, Leads, Avg CPL, Landing Page) + Internal Notes. This avoids the misleading "BoldLine pays itself $1,000/mo" display.
- **Package tab:** the package pricing/features/upgrade block is replaced by a short "Run Your Own Ads" note; the **Google + Meta launch cards still render** (gated by `pkg.platform`, which `c-growth` satisfies).

**How Bryson uses it (the E2E test):** open My Ads → Overview → Edit → enter BoldLine's own **Google Ads Customer ID** (+ approve the MCC manager link) → Package tab → Build Campaign. This is the FIRST real account linked to the MCC, so it's the live verification of `createCampaign` + ARIA approve→execute that had been untestable (see `google-ads-api`). Meta side waits on Meta App Review like everything Meta.

**Verified 2026-07-24 (headless):** OS compiles clean; dashboard shows the My Ads card + sidebar item, MRR/count exclude the house account (MRR $600 with 1 real client, not $1,600/2); internal detail has no Client-View/Contract tabs, MY ADS badge, cleaned Overview, and the Google launch card present on its Package tab.

**Lead → meeting BOOKING outreach (2026-07-24, Bryson: don't auto-convert a lead to a client — that skips the sales call and looks unprofessional; instead help contact leads + book a meeting).** The funnel is: ad lead → Bryson reaches out → they book a call → Bryson closes on the call → THEN they become a client (kept manual on purpose). Built into `LeadsTabContent`, **gated `client.internal`** (a regular client's leads are their own customers, not BoldLine prospects — they don't get this):
- **Sales pipeline** gains a **"Meeting Booked"** stage: New → Contacted → Meeting Booked → Won → Lost (regular clients keep the plain New/Contacted/Won/Lost).
- **"📅 Reach Out & Book"** per lead opens a panel with an **auto-drafted, editable** message personalized from the lead (first name, business, and their own message quoted) + the Calendly link. Send is **personal, not automated**: one-tap **Email** (`mailto:` prefilled subject+body) and **Text** (`sms:` prefilled, shorter body) open from Bryson's own address/phone — professional, and dodges the dormant branded-email deliverability problem (see `branded-lead-email-dormant`). Plus Copy / Copy-booking-link, and quick "Mark Contacted" / "Meeting Booked" status buttons.
- `CALENDLY_URL` constant (`https://calendly.com/theboldlinemedia/30min`, public, same link as BLOG_FACTS / marketing site). No new function, no AI call — instant smart template; AI personalization could be a later add.
- Verified headless: panel renders in the house account's Leads tab with a seeded lead, Email/Text/Calendly/Meeting-Booked all present, compiles clean.
- **~~NOT built~~ → BUILT 2026-08-07:** the acquisition-ROI view (ad spend → leads → clients won → CAC) now exists as `MyAdsInsights` (see the 2026-08-07 section below).

**START/STOP ADS + house-account cleanup pass (2026-07-24, Bryson: "no way to tell the system to start my ads" + "the edit sheet shows contracts for internal — fix that and review for anything else like this").**
- **`LiveCampaignsCard` (Campaigns tab, internal-only):** the launch cards only BUILD campaigns (paused); this is how you turn them ON. Lists the linked account's real campaigns (Google + Meta) with 30-day metrics and a **▶ Start Ads / ⏸ Pause** toggle per campaign. Start is **confirmed** (it begins real spend). Reads via `gadsCall`/`metaCall` `campaigns`, writes via `setStatus` (`ENABLED`/`PAUSED` for Google, `ACTIVE`/`PAUSED` for Meta) — the same functions ARIA's approval queue uses, now exposed as a direct owner control. Empty/no-account states guide you ("link on Overview → Edit, then build below"). Gated to `client.internal`; regular clients keep the ARIA-approval path.
- **GAP FOUND + FIXED:** the Landing-Page generator, Media library, and Call-Tracking tools live in the **"Client View" (portal) tab** — which was hidden for internal, so the house account had **no way to make a landing page for its ads.** Fix: for internal, the portal tab is **kept but renamed "Assets"**, and inside it the client-only cards (Live Client View portal preview + Client Setup Checklist) are hidden; the Landing Page / Media / Call Tracking / Lead Webhook cards stay (client-portal wording reworded for self). So internal tabs are now: **Overview · Campaigns · Assets · Leads · Reports** (Assets = ex-"Client View"; the **Log** tab was removed 2026-08-07 — no client to log comms with).
- **Edit sheet (`EditClientSheet`) for internal:** the **Status section (contract + intake) and the Pipeline section are removed** (this was the reported "contracts spot that shouldn't be there"), the **Health Score** card is hidden, the Ad-Account-Linking blurb + email label + header ("Edit My Ads Account") + delete copy are reworded for self. Sections are just **Info · Campaign · Notes**.
- Verified headless: internal tabs correct; Assets tab has no Client-View/Checklist but has Generate-Landing/Media/Call/Webhook; Edit sheet shows Info/Campaign/Notes with **no contract fields**; LiveCampaignsCard renders; compiles clean at desktop.

**DUAL COPY note:** this is all OS-app (`index.html`) — the portal isn't involved (internal has no portal). No `portal.mjs` change.
**Platform selection + multi-niche + general ad (2026-07-24).**
- **Platform chips now actually drive the Campaigns tab (house account):** launch cards are gated by `client.platforms` for internal (was `pkg.platform`). Pick Google Ads / Meta Ads / Google + Meta in Edit → Info → Platforms Running and the matching builder(s) appear; pick neither and a "pick a platform" note shows. Internal platform chips are restricted to Google/Meta/Combo (the only platforms the OS runs) — TikTok/YouTube/Other are hidden for internal so you can't select something that does nothing.
- **Multiple niches as multiple ads:** `GoogleLaunchCard` (internal) gained a **"Who should this ad target?"** field + **✨ Fill copy** button using a new `agencySeed(audience)` generator (BoldLine advertising to WIN that kind of business). Type a niche (e.g. "Roofers") → Fill → the campaign name ("BoldLine Media — Roofers"), headlines, descriptions, and keywords auto-write niche-targeted. Build, change the niche, build again — each is its own campaign (all listed in LiveCampaignsCard with individual Start/Pause). Leave blank → a **general "any business owner"** campaign (`agencySeed("")` → agency keywords like "marketing agency", "google ads management"). Strategy note (Bryson asked): niche-first converts better + cheaper than a broad "grow your business" ad; a low-budget general campaign alongside is fine.
- **Landing URL default fixed:** for the house account both Google + Meta launch cards default the final URL to **https://boldlinemedia.com/get-started** (was the OS /lp page). Meta now has the SAME audience field (mirrored 2026-07-24): `metaAgencySeed(audience)` seeds name/headline/primaryText/description niche-targeted or general (blank). Meta niche targeting is via COPY for now (age/country available); interest-based audience targeting is a future backend enhancement. Meta launch still needs App Review before it runs on any account.

**Campaigns-tab setup block + niche catalog + Log removed (2026-08-07, Bryson).**
- **Log tab removed for the house account** (no client to log comms with): the `TABS` filter drops `log` for internal too. Internal tabs are now **Overview · Campaigns · Assets · Leads · Reports**.
- **New `MyAdsSetupCards` component on the Campaigns tab (internal-only)**, rendered right below the "Run Your Own Ads" intro and above `LiveCampaignsCard`. Three cards: (1) **Monthly Ad Budget** setter — a `$`-prefixed number input that writes `client.adBudget` as `"$N/mo"` and shows the equivalent **daily** figure (monthly ÷ 30.4), because the launch cards take a DAILY budget; (2) collapsible **Step 1 — Add your payment card** guide (Google: Billing → Payments → Add payment method; Meta: Billing & payments → Payment settings → Add payment method), mirroring the client portal's connect copy; (3) collapsible **Step 2 — Connect your accounts to the bots (the ID numbers)** guide (where to find the Google Ads Customer ID / Meta Ad Account ID / Page ID) **with the three ID inputs inline** + a Save button — same fields as Edit → Campaign's "Ad Account Linking", both persist via `onUpdate` so they stay in sync. (Edit → Campaign still has its `adBudget` + linking fields too — intentional dual entry, harmless.) Since it's BoldLine's own account it already sits in the MCC / Business Manager, so the guide says no manager-request approval is needed — just paste the IDs.
- **Niche catalog expanded + custom entry (app-wide, not just My Ads).** `NICHES` (was the 4-item list) is now `Object.values(NICHE_GROUPS).flat()` — a grouped ~100-niche catalog (Home Services, Health & Medical, Beauty, Legal & Financial, Professional & Local, Fitness, Food & Hospitality, Retail & E-Commerce incl. clothing/jewelry/cosmetics brands, Education & Family, B2B & Other). New reusable **`NicheSelect`** component (grouped `<optgroup>` dropdown + a "+ Add a custom niche…" option that swaps to a free-text input, with a "List" button to switch back) replaces the plain niche `<select>` in **EditClientSheet** + **AddClientSheet**. The four original strings (Roofing, Med Spa, Auto Detailing, E-Commerce) are kept **verbatim** so their tailored ad angles (the `nd` map) + per-lead rates still match; any other/custom niche falls back to generic angles + the default per-lead fee — safe, since `nd` and `PER_LEAD` already have `|| default`. HomeScreen's niche **filter chips** now derive from the niches clients actually have (not the full catalog) so a 100-item list doesn't bloat the filter.
- **Verified 2026-08-07:** full `index.html` babel-compiles clean; `NicheSelect` (dropdown + custom modes) and `MyAdsSetupCards` (budget + both expanded guides + inline ID inputs) SSR-render without error and Playwright-measured **0px horizontal overflow at 390px** (desktop 560/1280 clean too).

**Campaigns-tab insights — checklist + budget pacing + acquisition ROI (2026-08-07, Bryson: "add all 3").** New **`MyAdsInsights`** component (internal-only), rendered at the TOP of the Campaigns tab (after the "Run Your Own Ads" intro, above `MyAdsSetupCards`). Fetches the house account's live Google+Meta campaigns ONCE (`gadsCall`/`metaCall` `action:"campaigns"`, same as `LiveCampaignsCard`) for spend / pacing / "is a campaign live"; the funnel comes from `client.leadsLog` (the account's own prospects).
- **Start Here checklist** — 5 steps: budget set / payment card added / accounts connected / landing page published / a campaign live. Auto-detected from client fields + live campaigns, EXCEPT "payment card" which is a manual check stored at **`client.myAdsSetup.cardAdded`** (the checkbox / "mark done" toggle persists via `onUpdate`). Progress bar + N/5.
- **Budget pacing** — Monthly budget (parsed from `adBudget`) vs **Live daily** (sum of ENABLED/ACTIVE `dailyBudget`) × 30.4 = projected monthly, vs **30d actual spend** (sum of Google `cost` + Meta `spend`). Colored bar (green/amber/red) + an **⚠ Over-budget RED banner at the very top of the tab** when projected > budget. That banner IS the "pacing alert" — it's in-app on the Campaigns tab; a phone/bell PUSH would need a scheduled backend check of live spend vs budget (not built — offered as a follow-up).
- **Acquisition ROI funnel** — all-time **Leads → Calls booked** (status `meeting`|`won`) **→ Clients won** (status `won`) with conversion %. Ad economics: 30d spend; **cost/lead** = 30d spend ÷ leads with `receivedAt` in the last 30d; **projected cost per client** = cost-per-lead ÷ close-rate (won/leads), honestly labeled "projected at your close rate" (a TRUE CAC needs cumulative spend, but the ad APIs only expose 30d — so this is a labeled projection, never presented as actual). Empty states are graceful (no accounts → "connect below"; no leads → prompt).
- **Verified 2026-08-07:** full `index.html` babel-compiles; SSR-rendered empty + populated (seeded live campaigns + a 12-lead `leadsLog`) — over-budget banner, 5/5 checklist, funnel (12 leads → 5 booked → 2 won), $1,022 30d spend, $85/lead, $511 projected CAC all compute correctly; Playwright-measured **0px overflow at 390px**.
