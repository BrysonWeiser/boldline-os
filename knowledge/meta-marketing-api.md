---
name: meta-marketing-api
topic: OS app
task: continue the Meta (Facebook) Marketing API setup and business verification
keywords: [meta-marketing-api, business-portfolio, business-verification, ads_management, ads_read, app-review, marketing-api-access-tier, standard-access, development-tier, security-center, screencast]
status: verified
summary: App Review came back 2026-08-12 PARTIALLY APPROVED — ads_management, business_management, pages_show_list, pages_read_engagement, public_profile all APPROVED (everything the OS code actually calls). Rejected: (1) Marketing API Access Tier — "not a sufficient number of Ads API calls in the last 15 days", so the app is stuck on Development tier = own ad accounts only; (2) ads_read — screencast showed static/placeholder numbers, reviewer wants live impressions/clicks/reach/spend/conversions populating for one ad account, and wants told the app is server-to-server/system-user. PLAN: don't resubmit yet — run real Meta ads on the My Ads house account (allowed on Development tier today) for 15+ days to generate genuine API traffic + real numbers, drop ads_read from the resubmission (ads_management is a superset), then re-request the tier. Meta gates only Meta-ads clients — Google clients sellable regardless.
verified: 2026-08-12
---

> **What to BUILD the moment the tier is granted is tracked separately in KB
> `meta-parked-work`** — Meta creative split testing, then the conditions-triggered refresh
> that rides on it. Raise that list at approval rather than rediscovering it.

**🟨 APP REVIEW DECIDED 2026-08-12 — PARTIAL APPROVAL. 5 of 7 granted; the 2 rejections do NOT block the OS's own account, only CLIENT accounts.**

**APPROVED:** `ads_management`, `business_management`, `pages_show_list`, `pages_read_engagement`, `public_profile`.
**REJECTED (2):**
- **Marketing API Access Tier** — *"Our records do not show a sufficient number of Ads API calls in the last 15 days by this application. It is required that the application successfully integrate with the Ads API before it is approved for Marketing API standard access tier."*
- **`ads_read`** — *"Screencast Not Aligned with Use Case Details"*; reviewer note: *"We see static or placeholder numbers. Please re-record a live data fetch for ad performance (e.g. impressions, clicks, reach, spend, conversions) and show them populating in your app dashboard for the same ad account."* Plus a general instruction: *"If your app is a server-to-server app OR your app is using system user token to access Meta API, please indicate it in your next submission so that we're aware that frontend Meta login authentication flow is not visible."*

**WHAT THIS ACTUALLY COSTS US (analysis 2026-08-12):**
- **`ads_read` is a non-issue for the code.** `ads_management` is a superset — it covers reads as well as writes. `meta-ads.mjs` authenticates with ONE system-user token carrying `ads_management` and reads campaigns via `{act}/campaigns` + `{act}/insights`; nothing in the repo needs `ads_read` on its own. **DECISION: drop `ads_read` from the resubmission entirely** rather than fight the screencast — every extra permission requested is another rejection surface.
- **Marketing API Access Tier IS the real gate.** Without it the app sits on Meta's **Development access tier**: Marketing API calls only work on ad accounts BoldLine itself owns / the app's admins have a role on, with materially lower rate limits. That is *fine* for the **My Ads house account** (act_1045064901242944) — which is why Test/campaigns/createCampaign all worked during the demo — but **Standard tier is required before the OS can operate a paying CLIENT's ad account.** So: Meta-ads clients stay gated; Google-ads clients are completely unaffected (unchanged stance since 2026-07-23).
- **Why the call counter was empty:** `meta-ads.mjs` is only ever invoked from the browser when Bryson clicks something in the OS. There is **no scheduled Meta traffic** — no Netlify scheduled function touches Meta (`alerts-watch.mjs:16` still carries the TODO for live per-client spend). So outside of the one demo session the app made ~zero Ads API calls, exactly what Meta measured.

**▶ THE PATH TO STANDARD TIER (both rejections share one cure: real ad traffic on the house account).**
1. **Turn on real Meta ads on the My Ads house account** — permitted on Development tier today. Needs: Mercury card on the ad account, `metaAdAccountId` + `metaPageId` pasted in My Ads → Campaigns → Step 2, at least one **image** in the Assets → Media library (createCampaign hard-fails without one), then Build Campaign (creates PAUSED) → LiveCampaignsCard **▶ Start Ads**. Budget $5–10/day is enough — the point is API calls + non-zero metrics, not results. ~$105–210 over three weeks. Bonus: it's genuine lead-gen for BoldLine, supplementing cold calls.
2. **✅ BUILT 2026-08-12 — the scheduled performance sync** (`netlify/functions/ads-sync.mjs`, every 6h — see KB `ads-sync`). Calls now accumulate on their own whether or not Bryson opens the OS, and it doubles as the live-spend feed the budget-pacing alerts always needed (the `alerts-watch.mjs:16` TODO). It also **alerts him the first time the Meta read fails**, which is what protects the 15-day streak — a dead token would otherwise silently stop the clock exactly like the 2026-07-22 outage did. ⚠ **The traffic only starts once `metaAdAccountId` is set on a client/the house account** — with nothing linked, the job correctly skips every row and calls nothing.
3. **Wait 15+ days of continuous traffic**, then resubmit **Marketing API Access Tier only**. Realistic timing: ads on mid-August → resubmit early September.
4. **Re-record the screencast to the reviewer's spec:** ONE ad account named on screen the whole time; show the numbers *arriving* (click into the Campaigns tab / refresh, spinner, values populate) rather than an already-populated view; show impressions, clicks, reach, spend and conversions for that same account. The `MyAdsInsights` + `LiveCampaignsCard` views on the house account's Campaigns tab are the surfaces to film.
5. **Lead the reviewer instructions with the server-to-server declaration** — the reviewer asked for it explicitly. It was buried in the 2026-07-21 text; next time it goes FIRST: "server-to-server app, authenticates with a business system-user token, no Facebook Login for end users, therefore no frontend Meta auth flow appears in the screencast."

**FREE / NO-COST FOLLOW-UPS from this decision:**
- **The system-user token does NOT need regenerating.** Own-business assets never needed advanced access, which is why it already worked; approval changes nothing about the token. Confirm with ARIA → Deploy → **Test Meta Ads Connection** (the built-in `debug_token` scope introspection reports the deployed token's actual scopes on failure).
- **"Data access renewal is complete for BoldLine OS. No further action is required"** (email, same day) is pure housekeeping — Meta's annual data-access re-attestation, automatic here. Note it **recurs roughly annually**; if a renewal is ever NOT completed, API access is cut off.
- **NOW ACTIONABLE (was parked pending the decision):** swap mom's borrowed phone number for Bryson's own clean SIM + authenticator 2FA — accountscenter.facebook.com → Personal details → Contact info → add + verify the new number, then remove the borrowed one. The review is decided, so there is no longer any reason to leave it on.

**🟢🟢🟢 APP REVIEW SUBMITTED 2026-07-29 — "Review in progress."** After business_management's last "API test call" counter finally ticked to Completed (the `GET me/businesses` run in Graph API Explorer registered), every gated permission read Completed / the use case showed "Testing complete", all 5 submission sections (Verification, App settings, Allowed usage, Data handling, Reviewer instructions) were green, and Bryson clicked **Submit for review**. developers.facebook.com → Review → App Review now shows **Status: "Review in progress — Most submissions are reviewed within 20 days"**, with all 7 permissions listed under "New requests": pages_show_list, business_management, ads_read, pages_read_engagement, public_profile, ads_management, Marketing API Access Tier. NO EDITS during review. Result arrives by email (theboldlinemedia@gmail.com) + the dashboard.
- **The two grey rows at submit time were NOT blockers:** public_profile (default perm, no review needed — count is informational) and pages_show_list (already had 14 logged test calls). The "Testing complete" badge + green "Allowed usage" section + active blue Submit button confirmed readiness.
- **PENDING (post-decision):** (1) if APPROVED → swap mom's borrowed phone number for Bryson's own clean SIM + authenticator 2FA (Accounts Center → Personal details → Contact info) so he's self-sufficient; the Alert Inbox (2) badge can be checked then. (2) if Meta asks for changes/rejects → the reviewer note tells us what to fix; re-record/re-answer and resubmit. (3) After approval, Meta-ads clients can be fully managed; nothing else to build (token/code/createCampaign all verified).
- App ID is in the dashboard URL/card — keep it OUT of the repo (Netlify secret-scan). Business ID also visible; same rule.

**Status: IN PROGRESS** (started 2026-06-25). The longest approval pipeline of the four integrations (Business Verification + App Review can take weeks) — started early on purpose to run in parallel with the Google Ads Basic Access wait. Started **from scratch** (no prior Facebook/Meta presence).

**🟩 ACCOUNT MAP — CORRECTED 2026-07-27 (supersedes the 2026-07-19 two-account model below):** There is **only ONE Facebook personal account** — "Bryson Weiser", primary login **brysonaweiser@gmail.com**, phone **+1 602-784-4228**. Confirmed in Accounts Center (accountscenter.facebook.com → Profiles = a single "Bryson Weiser" profile; Contact info = brysonaweiser@gmail.com + 602 only; birthday Oct 11 2007). **`theboldlinemedia@gmail.com` is NOT a separate FB account** — it's the Business Portfolio's business-CONTACT email (and works as a login alias resolving to this same one account). So the Business Portfolio + the "BoldLine OS" dev app + the BoldLine Media Page are ALL under this single account. The "second admin invite to brysonaweiser (pending)" saga was a phantom — brysonaweiser IS the account, so you can't invite it as your own second admin (hence the endless "you're already in the Business Portfolio"). Net for the checkpoint: the 602 number is the ONLY number on the ONLY account, so it can't be "removed from another account" — the sole fix is adding a CLEAN number to THIS account (Accounts Center → Personal details → Contact info → Add phone number, verify via SIM) then retrying the dev Confirm Account. Do NOT remove 602 before a replacement is verified or the checkpoint has nothing to text.

**(SUPERSEDED 2026-07-19 two-account note, kept for history):**
- **Portfolio admin / the account Bryson normally logs in as = `theboldlinemedia@gmail.com`** (display name "Bryson Weiser", labeled "(you)", **Active**, Full access/Everything). This is the newer account (created ~2026-07-06) and the one to use for all portfolio + dev-app + verification work.
- **Second admin = `brysonaweiser@gmail.com`** (the older personal account) — **Full access, status "Invited"/pending** as of 2026-07-19. The invite is already sent; it just needs accepting **while logged in AS brysonaweiser** (incognito). The prior "you're already in the Business Portfolio" error was from trying to accept while logged in as theboldlinemedia (the admin can't accept its own invite). Do NOT re-send — accept the pending one. Open risk: whether Bryson has the brysonaweiser FB password.
- (Supersedes the tangled 2026-07-07 notes below. `lleatherboy@gmail.com` is NOT one of his logins.)
- **PARKED 2026-07-19 (Bryson):** repeated accept attempts kept hitting "you're already in the Business Portfolio" (the accept tab kept resolving to theboldlinemedia, not brysonaweiser). Since a second admin is lockout-insurance and NOT a blocker for verification/first client, we stopped burning time on it — the invite stays pending and accept-able anytime. Revisit only when the brysonaweiser login is confirmed and it can be done in one clean incognito session. Moved on to the dev app.

**🔴 APP API ACCESS BLOCKED 2026-07-22 (morning) — demo recording ON HOLD until it clears.** Every Meta API call from the OS now returns **"API access blocked" (code 200, OAuthException)** — including the ads `test` call that was GREEN last night and even `debug_token` with the app's own app-token. Simultaneously, Bryson's facebook login (theboldlinemedia) got checkpointed with SMS-to-602-784-4228, which Meta refuses ("used to verify too many accounts" — same flagged number from dev-account creation), and the dev dashboard showed an **Alert Inbox (1)** badge. Diagnosis: Meta's automation restricted the app/account cluster overnight (likely trigger: brand-new app published Live + immediately creating campaigns; same class as the 2026-07-08 portfolio restriction, which cleared in ~24-48h via Request review). NOT a token/env/code problem — the morning's Read Page "API access blocked" was probably this block all along, so the token-swap debugging (delete/recreate META_SYSTEM_USER_TOKEN, added META_APP_ID env var) may have been chasing a ghost. RECOVERY: (1) stop ALL Meta activity — no login retries, no API-button clicks; (2) when login recovers (try "another way"/email code; the flagged phone is a dead end; last resort facebook.com/login/identify with theboldlinemedia@gmail.com), check **Alert Inbox** + **business.facebook.com/business-support-home** for the restriction notice → **Request review** (ID/business confirmation; July-8 precedent cleared within hours); (3) after reinstatement, re-run Test card + Read Page BEFORE recording — if Read Page still fails with the app alive, then investigate the pages-permission surface (use-case app customization) for real. Code shipped during the debug (kept, all good): Read Page now uses the Page's own access token from me/accounts, error messages carry Meta code/subcode/type, and a scopes-introspection diagnostic (debug_token via META_APP_ID, else me/permissions) appends the deployed token's actual scopes on failure.

**🟢 DEMO SESSION IN PROGRESS 2026-07-21 (later):** ad account **"BoldLine Demo" `act_1045064901242944`** created (Mercury card, PAUSED); Page + ad account + system-user all assigned; **system-user token regenerated with pages_show_list + pages_read_engagement + pages_manage_ads** (on top of ads_management/ads_read/business_management) and swapped into Netlify `META_SYSTEM_USER_TOKEN` + redeployed; **app switched Development → Live ("Publish")**; **createCampaign dry-fire VERIFIED live** (PAUSED campaign `120248748729780637`). Demo client = **DetailKing ATL** (metaAdAccountId + metaPageId + an image set; landing URL uses boldlinemedia.com since the /lp generator link 404s — see `landing-page-issues`). NEW OS capability shipped this session: **"📄 Read Page"** button on MetaLaunchCard (page action → pages_show_list + pages_read_engagement) — deployed to main. Bryson is **recording the screencast (Phase E)** now. REMAINING = record → upload to the 5 permission screencast slots + reviewer-instructions doc → Submit (no edit after submit). **GOTCHA logged:** creatives fail with "app…in development mode. It must be in public" until the app is Published/Live (see `meta-ads-integration`).

**▶ DEMO-SESSION RUNBOOK (2026-07-21) — the ONLY thing left to submit Meta App Review.** ALL submission TEXT is done + saved as drafts on Meta: Verification ✅, App settings ✅ (icon 1024²/privacy+terms=boldlinemedia.com/data-deletion=/privacy/category "Business and pages"/app domain boldlinemedia.com/**Website platform Site URL = https://boldlinemedia.com/** — the real OS lives at boldlinemedia.netlify.app but the branded domain is used for consistency, noted in reviewer instructions), Allowed-usage descriptions for all 7 perms (Describe+Agree green; public_profile fully complete), Data handling ✅ (processors Netlify/Supabase/Anthropic all "IT solutions and services…"+US, Anthropic also "Analytics and measurements"; controller = BoldLine Media LLC / US; national-security request = No; policies = legality review + data minimization + documentation), Reviewer instructions text ✅ (FB Login = No; server-side/system-user explanation + testing steps). REMAINING = only (a) the **screencast** and (b) the **"performed API test calls"** checkmarks — both satisfied in ONE demo session:
1. **Create a BoldLine-owned Meta ad account** (business.facebook.com → Settings → Accounts → Ad accounts → Add → Create new; assign to Bryson + the "BoldLine OS API" system user). Add the **Mercury card** as payment. **KEEP EVERYTHING PAUSED = $0 spend, ever.**
2. **In the OS Edit sheet**, on a demo client set **`metaAdAccountId`** (the new act id, digits only) + **`metaPageId`** (the BoldLine Page id), a **landing page URL**, and **upload at least one image** to that client's media library (createCampaign now REQUIRES an image — fails fast otherwise).
3. **Claude dry-fires first:** before recording, have Bryson run the OS "📘 Launch Meta Campaign" once (or Claude reviews the response) to confirm createCampaign returns clean (OUTCOME_TRAFFIC objective, no bid_strategy conflict — fixes shipped 2026-07-21). Fix any live API error BEFORE recording.
4. **Screen-record** while running, in the OS: ARIA→Deploy "Test Meta Ads Connection" (green) → open the demo client → ARIA reads its Meta campaigns → **on the client Package tab, "📄 Read Page"** (reads the Page the ads publish to) → "Launch Meta Campaign" creates a PAUSED campaign → (optional) an ARIA-proposed budget/pause change approved in the queue. These are the REAL API calls that tick "performed API test calls": **test/campaigns = ads_read + business_management**; **📄 Read Page = pages_show_list (me/accounts) + pages_read_engagement ({page}?fields=…engagement)**; **Launch + approve = ads_management**.
   ⚠ **WHY the Read Page step exists (added 2026-07-21):** the earlier plan assumed createCampaign would tick pages_show_list + pages_read_engagement because the creative carries a page_id — it does NOT. Passing a page_id into object_story_spec is an ads-publish (ads_management) action, not a Page READ, so Meta logs no pages_* call from it. The new **`page` action** in meta-ads.mjs (me/accounts + {page}?fields=name,fan_count,followers_count,engagement) is what genuinely exercises both page permissions, surfaced as the "📄 Read Page" button on MetaLaunchCard. Without it, pages_show_list + pages_read_engagement can't go green.
5. **Upload that recording** to Reviewer-instructions → documents-web-1 AND to the screencast slot inside each Allowed-usage permission (pages_show_list, business_management, ads_read, pages_read_engagement, ads_management).
6. Confirm every checklist item is green → **Submit for review** (NO edit/cancel after submit — get it right first). Review takes days–weeks; result by email.
OS wiring is all built: metaCall helper, per-client metaAdAccountId/metaPageId Edit fields, ARIA Meta reads, "📘 Launch Meta Campaign" panel, Meta approve→execute. Only blocks Meta-ads clients; Google clients unaffected.

**APP REVIEW — IN PROGRESS 2026-07-21.** App basics done (icon 1024², privacy/terms URLs = boldlinemedia.com/privacy + /terms, data-deletion URL = /privacy, category "Business and pages", app domain boldlinemedia.com — banner clears on HARD REFRESH, it's cache-stale). Submission checklist (Review→App Review): ✅ Verification, ✅ App settings, then **Allowed usage** (per-permission: Describe + screencast + "performed API test calls" + agree — Meta SAVES PARTIAL DRAFTS, so descriptions banked now, screencasts+API-calls need the demo), **Data handling**, **Reviewer instructions** (screencast). Permission set (auto-added by the "Create & manage ads" use case; dep chain pages_show_list→pages_read_engagement→ads_management): marketing-api-tier, pages_show_list, business_management, ads_read, pages_read_engagement, public_profile, ads_management. **Can't finish without a DEMO SESSION:** create a BoldLine-owned ad account (Mercury card, keep PAUSED=$0) + link it + the BoldLine Page to a demo client in the OS → run Test/campaigns/createCampaign (satisfies "API test calls" per permission) WHILE screen-recording (the screencast) → then Data handling + Reviewer instructions → submit (NO edit/cancel after submit). The per-permission DESCRIPTION drafts live in the 2026-07-21 chat (and are saved into Meta's forms as drafts). ads_management desc emphasizes: human-approval queue before any change, all campaigns created PAUSED, client owns+pays for their own account, no consumer login.

**createCampaign PRE-FLIGHT FIXES 2026-07-21** (dry-run review before App-Review screencast; couldn't hit live API — no ad account yet): (1) objective **OUTCOME_LEADS → OUTCOME_TRAFFIC** — OUTCOME_LEADS optimizing LINK_CLICKS needs a pixel/promoted_object and gets rejected; TRAFFIC is the correct pixel-free objective for BoldLine's clicks-to-landing-page model. (2) removed **ad-set `bid_strategy`** (Meta rejects it when campaign CBO budget is set — it's inherited from the campaign). (3) **image now required** — Meta link ads must have one; createCampaign fails fast with "upload an image" before creating any objects (was orphaning campaign+adset on the deep failure). So the demo/first real campaign MUST have an image in the client's media library. STILL UNVERIFIED against a live account — dry-run against a real ad account before recording the screencast.

**✅ META INTEGRATION CONNECTED 2026-07-20** — `META_SYSTEM_USER_TOKEN` (system user "BoldLine OS API", ads_management, never-expire) + `META_APP_SECRET` set in Netlify; redeployed; the OS ARIA→Deploy "Test Meta Ads Connection" card returns green "✓ Authenticated (Graph v25.0), 0 ad accounts visible" (0 = no ad account created yet, auth works). Code = `netlify/functions/meta-ads.mjs` (test/campaigns/setBudget/setStatus/createCampaign + image upload; Graph v25.0, `META_GRAPH_VERSION` override). Part A DONE. **REMAINING (Part B, to manage CLIENT accounts):** (1) Access verification (Tech Provider, ~5-day) — start in Security Center; (2) App Review for **Advanced Access `ads_management`** — needs app basics (privacy URL ✅ boldlinemedia.com/privacy), a BoldLine-owned ad account + one PAUSED test campaign to demo (Mercury card, $0 since paused), a use-case write-up + a screencast (drafts in chat 2026-07-20). Only gates Meta-ads clients.

**✅✅ BUSINESS VERIFICATION APPROVED 2026-07-19** (~17h after submit — far faster than the ~2-day estimate; email "Verification successful for BOLDLINE MEDIA LLC" to theboldlinemedia@gmail.com, "now verified, all admins notified"). Also got a "Getting Started with Marketing API" welcome email confirming app **BoldLine OS** (App ID visible in that email + on the app dashboard — will become the `META_APP_ID` env var; **keep the value out of the repo**, Netlify secret-scan). NEXT: (1) **Access verification** (Tech-Provider step, ~5-day review) — should be startable now that business verification cleared; (2) **App Review** for advanced `ads_management` — needs the Meta Marketing API integration actually BUILT into the OS first (nothing to demo yet — no Meta functions exist, only google-ads.mjs). Only gates Meta-ads clients; Google clients unaffected.

**(historical) BUSINESS VERIFICATION SUBMITTED 2026-07-19 — was "In review" (~2 business days est.).** Ran it via the app's Tech-Provider flow → Security Center → Business Verification. Path taken: became a **Tech Provider** (irreversible; correct — BoldLine accesses clients' ad accounts to provide a service); business type = **Private Company (LLC)**; searched by **EIN** → Meta returned TWO identical public records (both "BOLDLINE MEDIA LLC", 3101 N Central Ave Ste 183 #7182 Phoenix AZ 85012, same EIN, principal Bryson Weiser) differing ONLY in ZIP+4 (85012-2645 vs -3616); the Articles show only 85012 (no +4) so either was safe — picked **-2645**. Reviewed name/address/EIN (all matched the Articles) → submitted. NEXT after it clears: **Access verification** (Tech-Provider step, ~5-day review) then **App Review** for `ads_management`. (Articles "Character of Business" = "MARKETING AGENCY THAT RUNS ADS FOR COMPANIES" — helps App Review.) GOTCHA for future doc flows: the EIN search may return multiple public records that differ only by ZIP+4 not present on the state filing — any is fine, ZIP+4 isn't a verification criterion.

**✅ DEV APP CREATED 2026-07-19** — Meta app **"BoldLine OS"** (contact `theboldlinemedia@gmail.com`), created via business.facebook.com → Settings → Apps → +Add → "Create a new app ID". Use case = **"Create & manage ads with Marketing API"** (the exact match — programmatic create/manage/optimize campaigns; sets up the `ads_management` App-Review path). Connected to the **BoldLine Media** portfolio (shown "Unverified business" — expected). App ID on the app dashboard; App Secret NOT yet stored (env vars come at integration time). NOTE — creating the developer account first required **credit-card identity verification** (the phone `602-784-4228` was rejected as "already added" to a profile; card path used instead — identity-only, ~$1 temp auth, NOT ad billing). NOT touched yet: App Review, permissions, tokens.
- **Env vars (future, at integration time):** `META_APP_ID`, `META_APP_SECRET`, plus a long-lived/system-user token. Same hard constraint — clients link + pay for their own ad accounts; BoldLine holds manager access only.

**GOTCHA — Security Center says "Your organization does not need to be verified" (2026-07-19):** the Start-verification button does NOT exist until some Meta surface *requires* verification. For an API build, the trigger is the **developer app**: create the Business-type app at developers.facebook.com, connect it to the BoldLine Media portfolio, add the Marketing API product — then Security Center flips to offering Start verification. (The card's "Authorizations and verifications" link is worth one click first — Meta sometimes allows self-serve start from that hub.) Don't burn time hunting for the button in Security Center before the app exists.

**Setup path (click-by-click):**
1. Personal Facebook login (the admin "key" behind any business; it stays separate from the business) — use brysonaweiser@gmail.com.
2. **Business Portfolio** at business.facebook.com — name `BoldLine Media`, email brysonaweiser@gmail.com, confirm via the email link.
3. **Facebook Page** `BoldLine Media`, category Marketing Agency (needed for ads + verification).
4. **Business Verification** (Business Settings → Security Center) — the weeks-long step; submit ASAP to start the clock.
5. **Developer app** at developers.facebook.com (Business-type) + add the **Marketing API** product.
6. **App Review** for advanced `ads_management` access.

**Current position (2026-07-07): Business Portfolio CREATED.** The June "steps 1-3" had never completed. Bryson's personal Facebook account is BRAND NEW (created 2026-07-06, brysonaweiser@gmail.com); Meta first blocked portfolio creation with "your Facebook account is too new — try again in an hour," and the retry ~1h later succeeded. Portfolio: business name `BoldLine Media`, business email `theboldlinemedia@gmail.com` (contact address only; the personal login is the admin key). **Risk + mitigation:** a days-old admin account doing business actions is checkpoint-prone, so Bryson's OLDER personal account (lleatherboy@gmail.com — check real name + good standing) should be a SECOND full-control admin (Settings → People → Invite people). **QUEUED FOR 2026-07-15 (Bryson):** (1) retry the second-admin invite (Settings → People →
Invite people → brysonaweiser@gmail.com → Full control; a week has passed since the ageing
block — if it fails again, just wait longer); (2) **START BUSINESS VERIFICATION** (Security
Center → Start verification) — fully unblocked: LLC confirmed, docs already in hand from the
Mercury/Stripe round (Articles of Organization + EIN letter). Use the EXACT legal name, same as
Stripe/Mercury. Walk him through click-by-click and screenshot anything ambiguous.

**PAGE CREATED + DRESSED 2026-07-08** — Facebook Page `BoldLine Media` (category Marketing Agency) exists in the portfolio with: profile photo (gold B on brand dark, square 500px), cover (headline "BoldLine Media" + pain-point line "Paying for clicks that never become customers?" + "Book a call at boldlinemedia.com" — Bryson's mix, replaced a services-list draft), bio ("Google Ads and Meta Ads management with custom landing pages. Your ad account stays yours."), description, website + email. **Both PNGs committed at `docs/social-assets/`** (reusable for Google Business Profile etc.). Regen recipe: headless-chromium screenshot of HTML, bg #080A0F, gold #C8A84B, Playfair Display 600 + Inter 400 embedded as base64 woff2 (Google Fonts fetchable via the sandbox proxy with a browser UA). Remaining Meta steps: Business Verification (Security Center — UNBLOCKED 2026-07-08: BoldLine Media IS a registered LLC; gather formation/Articles docs + IRS EIN letter + a statement showing the business name/address), second-admin invite retry (~2026-07-14), then dev app + Marketing API + App Review.

**RESTRICTION LIFTED 2026-07-08 — portfolio REINSTATED** (email confirmation) within hours of the review request; the ID/identity review cleared it. Resuming setup: Page creation next, then Business Verification (gated on the LLC/EIN answer), second-admin invite still deferred ~a week. Pace actions — no bursts.

**Original restriction, for the record — 2026-07-08 ("We restricted your business" / "Business Account Not Allowed to Advertise")** — surfaced when Page creation was attempted; the "Why this happened" text is Meta's generic anti-automation boilerplate, i.e. the brand-new-account pattern got auto-flagged (portfolio + 2 invite attempts + Page attempt within ~an hour of creation). NOT a policy violation by us and NOT permanent: the restriction page (Account Overview → Business portfolio, or business.facebook.com/business-support-home) has a **Request review** button (179-day window). Review requested via that button (usually asks for ID/identity confirmation; decision typically 24-48h, email + Business Support Home). **Everything Meta (Page creation, verification, invites) is paused until the review clears — do not retry actions meanwhile.** Ignore the "Chat with Meta AI" popup.

**Invite BLOCKED 2026-07-07** ("User Cannot be Added to Business") — confirmed to be the day-old-portfolio ageing restriction, since it failed identically with the CORRECT email. **PENDING: retry the invite in ~a week; don't spam attempts.** Interim protection: 2FA on the new sole-admin account (Accounts Center → Password and security). **ACCOUNT MAP correction (2026-07-07):** the OLD/aged personal FB account = **brysonaweiser@gmail.com** (this is the one to invite as second admin); `lleatherboy@gmail.com` is NOT one of his Facebook logins (first invite mistakenly went there). The NEW FB account (created 2026-07-06, current sole portfolio admin) shows **theboldlinemedia@gmail.com** in the People list — its actual login email still to be confirmed with Bryson. Next: confirm the business-email verification link, create the BoldLine Media Facebook Page (category Marketing Agency), then Business Verification in Security Center. **Open question raised 2026-07-07: is BoldLine Media a registered entity (LLC/EIN)?** Verification wants legal name/address/phone + official docs (formation papers, EIN letter, or utility/bank statement in the business name) — if unregistered, that must be solved first (also needed for Stripe + contracts).

**No env vars yet** — App ID, App Secret, and a long-lived/system-user access token get added once the app exists. Same hard constraint: clients link and pay for their own ad accounts; BoldLine holds manager access only (see `business-constraint-ad-spend`).

**🔴🔴 DEV-ACCOUNT CONFIRMATION WALL 2026-07-23 — Meta App Review is HARD-BLOCKED on the flagged phone, no self-serve path.** Recovery attempt this morning: FB login itself now works (theboldlinemedia) and business-support-home shows "No account or asset issues" (ad account/business are FINE). BUT the **developer account** shows "Account confirmation needed — unusual activity… Confirm Account", and that flow dead-ends at a **Security check that ONLY offers SMS to (602) 784-4228**, which returns "This number has been used to verify too many accounts." There is **NO "try another way"/alternate-method link**, and Bryson has **no other phone number**. So the app (and thus App Review) cannot proceed until either (a) the "too many accounts" phone flag COOLS (these ease after days–weeks — retry the Confirm Account then), or (b) a CLEAN, unused-on-Meta **real mobile number** is available (cheapest reliable route: a ~$10 prepaid SIM from Walmart/Target/gas station, or borrow a friend/family phone for ONE code; Google Voice/VoIP usually rejected). Do NOT hammer "Send login code" — repeated attempts worsen the flag. **KEY PERSPECTIVE (do not let this stall the business): Meta App Review only gates META-ads clients; a GOOGLE-ads client can be sold + onboarded TODAY, unaffected.** Plan: park Meta ~a few days, retry the Confirm Account when the flag eases or a clean number exists; meanwhile pursue a Google client + the queued OS builds (Google launch card, GA4-in-OS). Everything Meta-side is otherwise done + verified (token/code/createCampaign) — nothing to rebuild; this is purely the account-confirmation gate.

**🟢🟢 UNBLOCKED 2026-07-28 — dev-account confirmation CLEARED + API LIVE again.** Via the "Enter mobile number" step (clean number path), the "Account confirmation needed" wall cleared — My Apps now opens straight to the dashboard (BoldLine OS, Mode: Live, Business: BoldLine Media, Administrator; App ID visible on the card — keep it OUT of the repo per Netlify secret-scan). And the OS **ARIA→Deploy "Test Meta Ads Connection" returns GREEN: "✓ Authenticated (Graph v25.0). 1 ad account visible to the business: BoldLine Demo."** So the 2026-07-22 "API access blocked" is fully lifted and the system-user token works + sees the demo ad account (act_1045064901242944). **Phase 2 (verify API live) = DONE.** REMAINING = the App Review demo+submit only: dry-fire Read Page + Launch Meta Campaign on the demo client to confirm they return clean live → screen-record (Test → open demo client → ARIA reads Meta campaigns → 📄 Read Page → Launch Meta Campaign PAUSED → optional approve a budget/pause) → upload the recording to reviewer-instructions + each of the 5 permission screencast slots → Submit (NO edit after). All submission TEXT already drafted/saved on Meta (see the 2026-07-21 runbook below). Follow-up: after submit + approval, swap the borrowed phone for Bryson's own clean SIM + authenticator 2FA (see account notes) so he's self-sufficient.

**🟩 APP REVIEW — one counter-tick from SUBMIT (2026-07-28).** Confirmation cleared (mom's number) + API live. In App Review (Review→App Review→submission → Next → Allowed usage), the submission is the 7-item set (Marketing API Tier + pages_show_list + business_management + ads_read + pages_read_engagement + public_profile + ads_management). Verification/App settings/Data handling/Reviewer instructions all ✅ already. Screencast (the OS demo recording: Test connection → Read Page → Launch Meta Campaign) uploaded to all 5 permission slots → all green. **Last gap was business_management's "performed API test calls" (was 0 of 1)** — the OS `test` only called me/adaccounts (ads-scoped). Fix attempt 1: added a me/businesses read to meta-ads.mjs `test` (deployed) but it didn't register from the system-user token. Fix that WORKED: **ran `GET me/businesses` in Graph API Explorer** (Meta App=BoldLine OS, User Token w/ business_management granted) → returned the business successfully (298ms). That's the required business_management call. NOW WAITING on Meta's test-call counter to tick 0→1 ("can take up to 24h"), which greens the last box → then final review → **Submit** (no edits after). Everything else in the submission is already green. After approval: swap mom's borrowed number for Bryson's own SIM + authenticator 2FA. Graph API Explorer is the go-to if any other permission ever shows "0 of N API calls required."

**🔴 RETRY 2026-07-27 — still flagged (superseded by the 2026-07-28 unblock above).** Ran ONE clean Confirm-Account attempt after ~4 days cooling: developers.facebook.com → My Apps → "Account confirmation needed" → Confirm Account → Security check still ONLY offers SMS to (602) 784-4228, no "try another way". Selected it + Send login code ONCE → same hard error: **"This number has been used to verify too many accounts on Facebook. Please try a different number."** Flag has NOT cooled yet. Did not retry (avoids worsening it).
- **DECISION — do NOT delete/remake the dev account + app (Bryson floated it 2026-07-27; I advised against).** The block is a NUMBER-level flag, not an account/app problem — remaking asks for phone verification again → same 602 number → same wall, so it fixes nothing AND destroys verified work (Business Verification approved, the app with all 7 permission descriptions + data-handling + reviewer-instructions drafts, wired tokens, verified createCampaign, demo ad account + Page). Rapid delete-create-recreate is exactly the pattern Meta's anti-abuse punishes → risks MORE flags. Everything is intact + waiting; the ONLY missing piece is a clean number.
- **THE FIX (unchanged): a clean, unused-on-Meta real mobile number** — ~$10 prepaid SIM (Walmart/Target/gas station) is the reliable route (one SMS code, ~15 min, then we finish Meta same-day), or borrow a friend/family phone for one code; Google Voice/VoIP usually rejected. Alternatively wait longer for the 602 flag to cool and retry. Meanwhile Google-ads clients are fully sellable today — Meta gates only Meta clients.
- **CHECKPOINT EVOLVED 2026-07-27 (RESUME HERE) — now accepts a TYPED number.** After re-confirming the 602 number on the personal account (Accounts Center), the dev "Confirm Account" flow advanced from the locked-radio Security check to an **"Enter mobile number"** screen (country-code + free-entry Mobile number box, "send a code via **WhatsApp or SMS**", Send code button). Typing **602** there still hard-fails "used to verify too many accounts" (confirmed on this surface too). BUT the open field means **any clean number types right in** — no hidden menu needed. **PLAN (paused by Bryson to save usage credits, resume when credits reset): enter his MOM's mobile number here → she relays the SMS/WhatsApp code → enter it → clears the "Account confirmation needed" wall.** Caveat told to Bryson: this adds her number to his account (reversible via Accounts Center → Contact info; leave it on until App Review is submitted). If mom's number also errors, fall back to a ~$10 prepaid SIM. THEN immediately: Phase 2 (re-verify the app/API works — ARIA→Deploy "Test Meta Ads Connection" green; the app/API was blocked back on 2026-07-22, so confirm it's live now) → Phase 3 (the screen-recorded dry-fire: Test → Read Page → Build Campaign on the demo client, using the already-created demo ad account act_1045064901242944 + DetailKing ATL, needs an image) → upload the recording to the 5 permission slots + reviewer-instructions → **Submit App Review** (no edit after submit). All submission TEXT is already drafted/saved on Meta.
- **(earlier same day) CHECKPOINT UI 2026-07-27:** the FIRST Security check offered ONLY the on-file 602 number — **no "use a different number", no email, no ID-upload option**, jumped straight to the number. So a clean SIM alone isn't enough; you must first **swap the number on the Facebook account** so the checkpoint has a new number to text. PROCEDURE for the retry (once a clean SIM exists): accountscenter.facebook.com → Password and security / Personal details → Contact info → Phone numbers → ADD + verify the new number (code to the SIM) → remove/replace the 602 number → THEN developers.facebook.com → Confirm Account → Security check should now text the new number. Caveat: Meta checkpoints can cling to the old number even after the swap; if so, only the flag cooling (weeks) unblocks it.

**SUPPORT-TICKET ROUTE = DEAD END (confirmed 2026-07-23).** Tried developer support → "Errors while submitting for App Review" → "Report a bug" → landed on **"No access to Platform Bug Reporting Tool"** (requires an Admin/Developer role on an app >24h old — access the unconfirmed/restricted dev account doesn't currently grant). Catch-22: the confirmation block strips the access needed to file a report about the confirmation block. Only pointer offered is the public Developer Community Forum (won't resolve an account-confirmation flag). CONCLUSION: no viable Meta support channel while unconfirmed. The ONLY real paths remain (a) wait for the "too many accounts" phone flag to cool then retry Confirm Account, or (b) a clean unused-on-Meta mobile number. Do not re-attempt the support-ticket route.


## 📅 2026-08-28 — RESUBMISSION SCHEDULED, AND WHY NOT SOONER

His Meta campaign started **20 Aug 2026** at about $7/day. That puts the 15-day API-activity
bar (the reason the tier request was refused) at **4 Sep**. The `ads-sync` job also hits Meta
**hourly** for the linked house account, so real API traffic has been accumulating on its own
regardless of spend.

**The ads_read rejection is already fixed by circumstance.** That one failed because the
screencast showed static placeholder numbers; the reviewer wanted live impressions, clicks,
reach, spend **and conversions** for a real account. By 28 Aug the house account had genuine
numbers flowing into the OS: **4,360 impressions, 101 clicks (2.3% CTR, roughly double the
Meta norm), 88 landing page views, $52 spent**. Record the My Ads → Live Ad Performance card,
not a mock.

**🔴 One column is still empty, and it is the one the reviewer named: CONVERSIONS.** Zero, at
28 Aug. So the deliberate choice was **not** to resubmit at the 4 Sep earliest date, but to
wait past it: the ads landing page was rebuilt the same day (see KB `ads-page-conversion`) to
swap a 30-minute-call ask for a two-field free audit, and the resubmission is worth far more
showing a campaign that **produces** than one that merely runs. A rejection costs weeks; ten
more days costs nothing.

**Scheduled: Thu 10 Sep 2026, 09:00 Phoenix** — trigger `trig_01HdzRpY84831ECeuqgCiJAc`,
fires a fresh session with the full brief. **The reminder is a backstop, not the record.**

**Two changes to make on the form when resubmitting:**
1. **Drop the `ads_read` request entirely.** `ads_management` is already approved and is a
   superset. Asking again only hands them something else to reject.
2. **Say explicitly that the app is server-to-server** — scheduled, system user, no end user
   ever logs in. The reviewer read it as a consumer app last time.

**If leads are STILL zero on 10 Sep**, do not simply wait again. Two weeks on the new page
with nothing is a real signal about the offer or the audience and deserves proper diagnosis.
Also reconsider resubmitting anyway at that point, since the API-call bar is long cleared and
the tier request may carry on that alone.
