---
name: google-ads-api
topic: OS app
task: connect, test, or debug the Google Ads API integration (MCC, OAuth, Basic Access, approval queue)
keywords: [google-ads.mjs, MCC, login-customer-id, GOOGLE_ADS_API_VERSION, redirect_uri_mismatch, basic-access, propose_action, pendingActions]
status: verified
summary: Google Ads is wired via one MCC + Developer Token + OAuth refresh token (login-customer-id header); code in google-ads.mjs (test/campaigns/setBudget/setStatus). Developer Token BASIC ACCESS APPROVED 2026-07-19 (15,000 ops/day) — the long-standing blocker is cleared. Use the Web-app OAuth client (Desktop got redirect_uri_mismatch). Remaining: confirm the Deploy-tab test card authenticates end-to-end, then wire approve→execute.
verified: 2026-07-19
---

**Architecture:** one **MCC** manager account (its ID is in `GOOGLE_ADS_MANAGER_CUSTOMER_ID`) + one Developer Token + one OAuth refresh token operate across all linked client accounts via the `login-customer-id` header. OAuth consent screen **published to PRODUCTION** (avoids Testing mode's 7-day refresh-token expiry).

**Which Google account (Bryson, 2026-07-07):** the MCC — and therefore the API Center / developer token / Basic Access application — lives under **theboldlinemedia@gmail.com** (the business gmail; also where Netlify Forms notifications go). It is NOT under brysonaweiser@gmail.com — that personal account holds **Search Console** (set up 2026-07-07) and the **Meta** setup. Sign into the right one before looking for the MCC.

**OAuth client gotcha:** two clients exist — a **Desktop-app** one (**unused**; OAuth Playground rejected it with `redirect_uri_mismatch`) and a **Web-application** one (**in use**; has `https://developers.google.com/oauthplayground` as an authorized redirect URI). The refresh token was generated against the **Web-app** client, so `GOOGLE_ADS_CLIENT_ID` / `GOOGLE_ADS_CLIENT_SECRET` must be the **Web-app** client's (refresh tokens are bound to the issuing client).

**Env vars:** `GOOGLE_ADS_DEVELOPER_TOKEN`, `GOOGLE_ADS_CLIENT_ID`, `GOOGLE_ADS_CLIENT_SECRET`, `GOOGLE_ADS_REFRESH_TOKEN`, `GOOGLE_ADS_MANAGER_CUSTOMER_ID` (stored **with dashes**; the code strips them). Optional `GOOGLE_ADS_API_VERSION` (defaults **`v24`** in code as of 2026-07-19; Google sunsets versions ~yearly — if `test` errors "version not found/deprecated," set this to a current version in Netlify, no code change).

**GOTCHA — sunset version = HTTP 404 (2026-07-19):** the first live test after Basic-Access approval failed with `test — test: HTTP 404 | {}`. That's NOT an auth failure — the function ran and Google returned 404 because the code default `v18` was sunset (the version is in the REST path `…/v18/customers:listAccessibleCustomers`, and a retired version's path 404s with an empty body). Fix = bump to a live version. Google keeps ~3 majors live (v22/v23/v24 in Jul 2026); default bumped to **v24** in `google-ads.mjs`. A 401/403 (not 404) with a populated `detail` would instead point at creds/token — different problem.

**Code:** `netlify/functions/google-ads.mjs` — OAuth refresh→access-token exchange, then actions `test` (listAccessibleCustomers smoke test), `campaigns` (GAQL read + 30-day metrics), `setBudget` + `setStatus` (guarded writes). Secured by the owner's Supabase session. A "Test Google Ads Connection" card on the Deploy tab (labeled "expected to fail until Basic Access is approved"; one-click, no redeploy needed to verify the moment the token lands). Per-client field `googleAdsCustomerId` in the data model for MCC linking.

**✅ BASIC ACCESS APPROVED — 2026-07-19** (email "Basic Access Approval" to theboldlinemedia@gmail.com, Manager Account 989-283-2533). The Developer Token is now **Basic Access = 15,000 API operations/day, free**, across real (non-test) accounts. The token STRING did not change on upgrade (Google approves the existing token in place — it's on API Center → Developer Details), so **no env-var change is needed** unless the test card reports an auth failure. This clears the June rejection (old Wix URL) — the 2026-07-07 resubmission against the live `boldlinemedia.com` + design doc succeeded.
- **Confirm it's live:** OS → **ARIA → Deploy tab → 📈 Test Google Ads Connection → Test**. A pass shows green "✓ Authenticated (API vXX). N account(s) visible to the manager…". (Was expected to fail under Explorer Access.) If it errors, the message includes the `stage` + detail — first suspects: `GOOGLE_ADS_API_VERSION` sunset (bump it in Netlify) or a stale refresh token.
- **Next automation step (code, not paid setup):** wire approve→execute — turn an approved `pendingAction` into a real `setBudget`/`setStatus` call. Executable pieces already exist in `google-ads.mjs`; needs live `campaigns` reads to resolve campaign + resource names first, and can only be E2E-verified once a real client ad account is linked to the MCC.

**RESUBMISSION HISTORY — SUBMITTED 2026-07-07, APPROVED 2026-07-19.** Form facts for any future application:
- **GOTCHA — the "Use past details" sidebar** offers to re-apply answers from the rejected June case (Case ID 24677441296), INCLUDING the old wixsite.com URL that caused the rejection. Never click "Apply selected fields" — Clear all and fill fresh.
- **A design document upload is REQUIRED (Q7, .pdf/.doc/.rtf)** — "if you don't have these ready, your application will be declined." A fresh doc was written 2026-07-07: source template committed at `docs/google-ads-api-design-doc.html` (MCC ID redacted to the placeholder `GOOGLE-ADS-MANAGER-ID-WITH-DASHES` — the real ID is an env-var value, committing it would trip Netlify's secret scanner). To regenerate: swap in the real ID, then `chrome --headless --disable-gpu --no-sandbox --print-to-pdf=out.pdf --no-pdf-header-footer file://<abs-path>.html` (Chromium at `/opt/pw-browsers/chromium-*/chrome-linux/chrome` in the sandbox). Contents: business model (client owns account, MCC manager access only), architecture (server-side Netlify functions, creds in env vars), exact API services used (listAccessibleCustomers, GoogleAdsService.search GAQL, mutateCampaignBudgets, mutateCampaigns), the human-approval queue for ALL writes, low volume estimate, data handling, RMF-not-applicable (internal tool).
- Answers used: contact email theboldlinemedia@gmail.com; MCC ID with dashes; website `https://boldlinemedia.com`; Q4 Google rep = No; Q6 = business-model paragraph + note that the earlier application referenced an outdated placeholder site; Q8 access = **Internal users - employees only**; Q9 someone else's tool = No; Q10 App Conversion Tracking = No. Form says reviews typically ~3 business days.
- The form's red box says to verify **API contact email + company type** in API Center → Developer Details BEFORE submitting (wrong company type delays review; Agency-type usage).

**Approve→execute deferred (correctly):** ARIA's `propose_action` logs a *descriptive* proposal (title/detail/category) to the `pendingActions` approval queue; `decideAction` records the approve/reject. This is Task #8's human-in-the-loop guardrail — **no automated process executes a real ad-account change without manual approval** — and it was the safety story cited in the Basic Access design doc. Turning an approval into a real `setBudget`/`setStatus` call needs live campaign reads first (to resolve the specific campaign + resource names), so it's a post-Basic-Access task. The executable pieces are ready in `google-ads.mjs`; only the final UI wire-up remains.
